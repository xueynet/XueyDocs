# IAccessToken interface

Access Token接口。

``` c#
public interface IAccessToken
```

## Members

| name | description |
| :----- | :----- |
|ExpiresAt { get; set; }	|到期时间。|
|UserId { get; set; }	|用户Id。|
|UserName { get; set; }	|用户名。|

